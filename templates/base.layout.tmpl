{{define "base"}}
    <html>
        <head>
            <title>Homepage</title>
        </head>
        <body>
            {{block "content" .}}

            {{end}}
        </body>
    </html>
{{end}}